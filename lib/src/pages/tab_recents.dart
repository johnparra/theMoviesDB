import 'package:flutter/material.dart';

class TabRecents extends StatelessWidget {
  const TabRecents({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Recientes'),
      ),
    );
  }
}
